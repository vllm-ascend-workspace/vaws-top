interface:
  display_name: "vaws-top"
  short_description: "Inspect observed state of local NPU fleet servers"
  default_prompt: "Use $vaws-top to inspect observed NPU fleet state from cache or with one live refresh; device allocation still comes from the coordinator."
